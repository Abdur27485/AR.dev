import React from 'react';

const SectionSecondaryText = ({ text }) => {
    return (
        <p className='text-[18px] md:text-[20px] text-gray-600 text-center mt-4 mb-6 md:mb-12'>
            {text}
        </p>
    );
};

export default SectionSecondaryText;